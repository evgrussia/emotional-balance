# emotional-balance — production deploy (balance-space.ru)
# Run from repo root: docker compose -f deploy/docker-compose.prod.yml up -d

services:
  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
    image: emotional-balance-frontend:latest
    container_name: emotional-balance-frontend
    restart: unless-stopped
    ports:
      - "8084:80"
